import React,{useEffect,useState} from 'react';
const API=process.env.REACT_APP_API_URL||'https://bangladesh-voter-python.onrender.com/api';
function Admin(){const[token,setToken]=useState(localStorage.getItem('admin_token')||''),[username,setUsername]=useState(''),[password,setPassword]=useState(''),[files,setFiles]=useState([]),[docs,setDocs]=useState([]),[message,setMessage]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const headers=()=>token?{Authorization:`Bearer ${token}`}:{};const loadDocs=async()=>{try{const r=await fetch(`${API}/admin/documents`,{headers:headers()});const d=await r.json();if(!r.ok)throw Error(d.detail||'Authentication required');setDocs(d)}catch(e){setError(e.message)}};useEffect(()=>{if(token)loadDocs()},[token]);const login=async e=>{e.preventDefault();setLoading(true);setError('');try{const r=await fetch(`${API}/admin/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const d=await r.json();if(!r.ok)throw Error(d.detail||'Login failed');localStorage.setItem('admin_token',d.token);setToken(d.token);setPassword('')}catch(e){setError(e.message)}finally{setLoading(false)}};const upload=async()=>{if(!files.length)return;setLoading(true);setError('');setMessage('PDF processing চলছে...');try{const fd=new FormData();[...files].forEach(f=>fd.append('files',f));const r=await fetch(`${API}/admin/upload-pdf`,{method:'POST',headers:headers(),body:fd});const d=await r.json();if(!r.ok)throw Error(d.detail||'Upload failed');setMessage('Upload completed');setFiles([]);document.getElementById('pdfFiles').value='';await loadDocs()}catch(e){setError(e.message)}finally{setLoading(false)}};const logout=()=>{localStorage.removeItem('admin_token');setToken('');setDocs([])};if(!token)return <main className="container" style={{maxWidth:520,margin:'60px auto'}}><section className="panel"><h2>🔐 Admin Login</h2>{error&&<div className="error">⚠️ {error}</div>}<form onSubmit={login}><div className="field"><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)} required/></div><div className="field" style={{marginTop:12}}><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="primary" style={{marginTop:16,width:'100%'}} disabled={loading}>{loading?'Login হচ্ছে...':'Login'}</button></form></section></main>;return <main className="container"><div className="panel"><div style={{display:'flex',justifyContent:'space-between'}}><div><h2>🛠️ Admin Dashboard</h2><p>PDF upload, OCR processing এবং document management</p></div><button className="secondary" onClick={logout}>Logout</button></div>{error&&<div className="error">⚠️ {error}</div>}{message&&<div style={{padding:12,margin:'12px 0',borderRadius:8,background:'#e8f5e9'}}>{message}</div>}<div className="field"><label>PDF files</label><input id="pdfFiles" type="file" accept="application/pdf,.pdf" multiple onChange={e=>setFiles(e.target.files)}/></div><button className="primary" style={{marginTop:12}} onClick={upload} disabled={loading||!files.length}>{loading?'Processing...':'📤 Upload PDF'}</button></div><section className="panel"><div style={{display:'flex',justifyContent:'space-between'}}><h3>📄 Documents</h3><button className="secondary" onClick={loadDocs}>Refresh</button></div>{docs.length===0?<p>No documents uploaded yet.</p>:<div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th>File</th><th>Status</th><th>Pages</th><th>OCR</th><th>Error details</th></tr></thead><tbody>{docs.map(d=><tr key={d.id}><td>{d.filename}</td><td>{d.status}</td><td>{d.page_count||'-'}</td><td>{d.ocr_used?'Yes':'No'}</td><td style={{maxWidth:500,whiteSpace:'pre-wrap',wordBreak:'break-word',color:d.status==='failed'?'#b00020':'inherit'}}>{d.error_msg||'-'}</td></tr>)}</tbody></table></div>}</section></main>}
function Search(){return <main className="container"><section className="panel"><h2>🇧🇩 ভোটার তথ্য অনুসন্ধান</h2><p>Admin dashboard থেকে PDF upload ও processing করুন।</p><a href="./admin">🔐 Admin Login</a></section></main>}
export default function App(){return window.location.pathname.endsWith('/admin')?<Admin/>:<Search/>}
